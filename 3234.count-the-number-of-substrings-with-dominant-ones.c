// @leet start
#include <stdio.h>
#include <string.h>
int numberOfSubstrings(char *s) {
  int zCount = 0;
  int oCount = 0;
  int result = 0;
  for (int i = 0; i < strlen(s); i++) {
    for (int j = i; j < strlen(s); j++) {
    }
  }
  return 0;
}
// @leet end
